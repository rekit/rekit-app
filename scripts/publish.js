/*
 * 1. Build Rekit Studio: yarn build
 * 2. Build Rekit Plugins: yarn build
 * 3. Build Rekit App static server: yarn build This will copy built-in plugins to build server
 * 4. Build Electron app: yarn dist
 * 
 * To test:
 * 1. Remove <user home>/.rekit folders
*/