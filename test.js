// const chokidar = require('chokidar');
// const watcher = chokidar.watch(__dirname, { ignored: /node_modules/ });
// watcher.on('all', (...args) => console.log('file changed', args));
// setInterval(() => {
console.log(new Date());
// }, 1000);
